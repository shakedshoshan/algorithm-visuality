const Flyweight = () => (
  <div className="max-w-2xl mx-auto p-8 bg-white rounded shadow">
    <h1 className="text-3xl font-bold mb-4">Flyweight Pattern</h1>
    <p className="text-lg text-gray-700">
      The Flyweight Pattern reduces memory usage by sharing as much data as possible with similar objects. It is especially useful when dealing with large numbers of similar objects.
    </p>
  </div>
);

export default Flyweight; 