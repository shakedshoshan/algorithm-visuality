const Bridge = () => (
  <div className="max-w-2xl mx-auto p-8 bg-white rounded shadow">
    <h1 className="text-3xl font-bold mb-4">Bridge Pattern</h1>
    <p className="text-lg text-gray-700">
      The Bridge Pattern decouples an abstraction from its implementation so that the two can vary independently. It is useful when both the abstractions and their implementations are likely to change.
    </p>
  </div>
);

export default Bridge; 