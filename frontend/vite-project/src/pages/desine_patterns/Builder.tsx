const Builder = () => (
  <div className="max-w-2xl mx-auto p-8 bg-white rounded shadow">
    <h1 className="text-3xl font-bold mb-4">Builder Pattern</h1>
    <p className="text-lg text-gray-700">
      The Builder Pattern separates the construction of a complex object from its representation, allowing the same construction process to create different representations. It is especially useful for creating objects with many optional parts.
    </p>
  </div>
);

export default Builder; 