const Prototype = () => (
  <div className="max-w-2xl mx-auto p-8 bg-white rounded shadow">
    <h1 className="text-3xl font-bold mb-4">Prototype Pattern</h1>
    <p className="text-lg text-gray-700">
      The Prototype Pattern creates new objects by copying an existing object, known as the prototype. This is useful when the cost of creating a new instance is more expensive than copying an existing one.
    </p>
  </div>
);

export default Prototype; 