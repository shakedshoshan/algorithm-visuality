const Factory = () => (
  <div className="max-w-2xl mx-auto p-8 bg-white rounded shadow">
    <h1 className="text-3xl font-bold mb-4">Factory Pattern</h1>
    <p className="text-lg text-gray-700">
      The Factory Pattern defines an interface for creating objects, but lets subclasses alter the type of objects that will be created. It promotes loose coupling by eliminating the need to bind application-specific classes into the code.
    </p>
  </div>
);

export default Factory; 